<ul class="navbar-nav ml-auto">
    @if (User.Identity.IsAuthenticated)
    {
        <!-- Info Showed when Logged in-->
        <li class="nav-item">
            <a class="nav-link text-white " asp-controller="Bookings" asp-action="MyBookings">
                Welcome @User.Identity.Name
            </a>
        </li>

        <li class="nav-item">
            <form class="form-inline text-white" asp-controller="Account" asp-action="Logout"
                    asp-route-returnUrl=@Url.Action("Index", "Home")>
                <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>
    }
    else
    {
        <!-- Info Showed when when not Logged in -->
        @*<li class="nav-item btn-group" role="group">
            <form class="form-inline text-white" asp-controller="Account" asp-action="Logout"
              asp-route-returnUrl=@Url.Action("Index", "Home")>
                <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>

        </li>*@
        <li class="nav-item btn-group" role="group">
            <a class="btn btn-secondary" asp-controller="Account" asp-action="Login">
                Login
            </a>
            <a class="btn btn-secondary" asp-controller="Register" asp-action="Index1">
                Register
            </a>
        </li>
    }
</ul>